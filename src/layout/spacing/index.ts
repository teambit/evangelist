import styles from './spacing.module.scss';

/**
 * EXPERIMENTAL.
 * CSS component to define commonly used spacing (between sections, items, etc).
 * @name spacing
 */

export const margin = {
	30: styles.margin30,
	50: styles.margin50,
	80: styles.margin80,
	130: styles.margin130,
	180: styles.margin180,
};
